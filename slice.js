// basically slice method will return the array of only selectted items for example (2,4) means 2 index r 2 index sy further jitny items hn wo dy do r 4 sy jo hn un ko discard kr do
const slicemethod = ["apple", "banana", "kivi", "orrange", "mango", "lemon"];
const result = slicemethod.slice(2, 4);
console.log("result", result);
